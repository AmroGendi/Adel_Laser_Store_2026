document.addEventListener('DOMContentLoaded',()=>{const root=document.querySelector('#categorySections');if(!root)return;root.innerHTML=Object.entries(cats).filter(([k])=>k!=='all').map(([k,n])=>{const ps=products.filter(p=>p.category===k).slice(0,4);return `<section class="category-block" id="${k}"><div class="section-head"><div><small>CATEGORY</small><h2 class="section-title">${n}</h2><p class="section-desc">${categoryMeta[k].desc}</p></div><a class="text-link" href="products.html?category=${k}">عرض كل المنتجات ←</a></div><div class="sub-links">${Object.entries(subs[k]).filter(([s])=>s!=='all').map(([s,t])=>`<a href="products.html?category=${k}&sub=${s}">${t}</a>`).join('')}</div><div class="products">${ps.map(card).join('')}</div></section>`}).join('')})
